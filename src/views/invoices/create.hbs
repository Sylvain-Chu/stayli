<h1>Créer une facture</h1>
{{#if bookings.length}}
  <form method='post' action='/invoices/create'>
    <div>
      <label for='bookingId'>Réservation</label>
      <select id='bookingId' name='bookingId' required>
        <option value='' disabled selected>Choisir une réservation éligible</option>
        {{#each bookings}}
          <option value='{{this.id}}'>
            {{this.property.name}}
            —
            {{this.client.firstName}}
            {{this.client.lastName}}
            — du
            {{this.startDate}}
            au
            {{this.endDate}}
          </option>
        {{/each}}
      </select>
    </div>
    <div>
      <label for='dueDate'>Date d'échéance</label>
      <input type='date' id='dueDate' name='dueDate' required />
    </div>
    <div>
      <label for='amount'>Montant</label>
      <input type='number' id='amount' name='amount' min='0' step='0.01' required />
    </div>
    <button type='submit'>Créer</button>
  </form>
{{else}}
  <p>Aucune réservation éligible disponible. Créez une réservation sans facture pour pouvoir émettre
    une facture.</p>
{{/if}}